# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environment
venv/
.venv/

# Environment variables (секреты — не коммитить; шаблоны в infra/environments.example/)
infra/.env
infra/.env.previous
infra/.environments/

# Локальный override compose (волюм с кодом для разработки; прод живёт без него)
infra/docker-compose.override.yml

# Django
db.sqlite3
*.log
staticfiles/
media/
celerybeat-schedule
backend/celerybeat-schedule

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Coverage
htmlcov/
.coverage
.coverage.*

# Build artifacts
*.bak
backend/.pytest_cache/
backend/static/
frontend-foody-front.bak/
.claude/scheduled_tasks.lock
.dev-media/
# Письма локальной разработки: коды подтверждения падают сюда файлами
.dev-mail/
# Выгрузка справочника — результат команды, а не исходник
catalog.xlsx
catalog-*.csv

# Исходники иконок: тяжёлые фотографии, из которых собираются иконки справочника.
# В репозиторий едут только уменьшенные копии — backend/catalog_icons/.
/icons/
